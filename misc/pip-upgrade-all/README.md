pip-upgrade-all - fresh packages won't remedy your mortality
============================================================
Upgrades all local pip packages for your default Python.

```
$ pip-upgrade-all
<...>
```
