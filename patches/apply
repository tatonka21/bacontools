#!/bin/sh

if [ "$(grep "Ubuntu" /etc/issue)" = "1" ]; then
	exit 0
elif [ "$(grep "Raspbian" /etc/issue)" = "1" ]; then
	git am "patches/0001-don-t-install-bananaglee-when-there-s-no-runghc.patch"
fi
