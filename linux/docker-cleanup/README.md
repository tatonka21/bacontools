docker-cleanup - scrubbing blood off the endless floors over and over
=====================================================================
`docker-cleanup` removes all old (weeks and months-old) Docker containers from
the system.
