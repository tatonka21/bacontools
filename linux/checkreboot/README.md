checkreboot - it won't make your server work anyway
===================================================

Checks if your machine requires reboot. Uses the reboot-required file provided
by unattended-upgrades.
